[package]
name = "bevy_project"
version = "0.0.0"
edition = "2021"
[dependencies]
bevy = { version = "0.12.1", features = ["dynamic_linking"] }
bevy_screen_diagnostics = "0.4.0"
[profile.dev]
opt-level = 1
[profile.dev.package."*"]
opt-level = 3
